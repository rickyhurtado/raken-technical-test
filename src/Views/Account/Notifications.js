import React, { Component } from 'react';
import { PageTitle } from '../../Lib/Common/Views';

export default class Notifications extends Component {
  render() {
    return (
      <div className="page">
        <PageTitle title="Notifications" appName="Account" />
      </div>
    );
  }
};
